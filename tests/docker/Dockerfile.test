FROM node:18

# Set working directory
WORKDIR /app

# Install the main package from npm registry
# Note: better-sqlite3 dependency requires build tools which are in node:18 (not slim)
RUN npm install agentic-robotics --verbose

# Copy test script
COPY test-remote-install.js /app/

# Run tests
CMD ["node", "test-remote-install.js"]
